# HM Dashboard (Streamlit)

## Run locally

```powershell
cd "D:\vs code\cursorProjects\HMdashboard"
py -m pip install -r requirements.txt
py -m streamlit run dashboard.py
```

Open `http://localhost:8501`.

## Stop the server
- In the terminal running Streamlit: press `Ctrl+C`